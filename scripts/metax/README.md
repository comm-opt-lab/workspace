# 沐曦脚本

`train50/` 来自 ts-tg200 `/home/yaowenxuan/ff/tj/tj-ff/train50`：FM9G / DeepSeek / Qwen 的 base vs DGC opt 启动脚本，以及 ALL_COMM 对比 CSV。

配套 Megatron 在 [megatron-metax](https://github.com/comm-opt-lab/megatron-metax)（子模块 `megatron/metax`，默认 `dgc-opt`，对应服务器 `/home/yaowenxuan/ff/tj/Megatron-LM-0.14.0-speedup`）。

不提交 dataset、ckpt、大体积 profiler trace。
